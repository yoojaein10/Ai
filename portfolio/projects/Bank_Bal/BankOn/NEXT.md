# 이어서 할 일 (2026-08-24 기준)

## 지금 상태 한 줄
추출·매핑은 실물 대조로 검증까지 끝났고(신한 4건 123일치/2불일치, 국민 3건 56일치/6불일치),
화면 이동(실행→로그인→검색→작성 폼 열기)도 동작한다. **남은 건 화면에 값을 넣는 입력 드라이버.**

## 2026-08-25 진행분 — 감정서번호 없이 실화면 시험(읽기 전용) 완료

- `cli rows [--from --to --work-type]`: 조건 조회 후 행 순회(`navigate.walk_rows/list_rows`)로
  목록 전부 읽기. **실측 38행, 감정서번호 38개 전부 다름** → Ctrl+A 1행 문제는 Down키 순회로 해결.
  Ctrl+C 텍스트는 "헤더줄 + 데이터줄" 2줄(탭 구분 40셀) — 데이터는 마지막 줄.
- `cli open/dump --row N [--from --to]`: 번호 없이 N번째 행을 열어 읽기(`navigate.focus_row`).
  1행(기업은행 담보 TBNKKIB24DAMB) 열기→덤프 5필드→스크린샷→'닫 기' 정상. 저장 없음.
- 러너: `tools/run_noid_test.py <from> <to> <row|0=rows만>` (관리자 권한, reports/noid_test_*).
- 기존 `--scan <번호>` 경로도 같은 walk_rows 를 쓰므로 번호만 있으면 바로 동작할 것.

- **드라이런 보고 러너** `tools/run_dryrun_test.py [from] [to]`(기본 8/10~8/18, 관리자): 조회 목록에서
  신한/국민 후보를 모아 APW로 .gam 유무를 확인해 첫 건을 열고 autofill_*(드라이런) 표를 남긴다.
  실측(2026-08-25, reports/dryrun_20260825_094718.*):
  - 8/18 접수건은 전부 .gam 없음(감정서 미작성) → 8/10~ 로 넓혀야 대상이 나옴.
  - 신한 01-2608-3-2594: .gam은 있으나 **속이 빈 골격**(mullist0=[], price=0) → 채울계획 4개뿐,
    **총감정가액=0을 채우려 함 → 0/빈값은 채우지 않는 가드 필요**(autofill 공통).
  - 국민 01-2608-3-2647(재의뢰, 사람이 이미 작성): 일치 16·채움 3·수동 7·미발견 8.
    ★`표준지소재지` 화면값을 등기번호(12441996507720)로 잘못 읽음 — 라벨 매칭이 옆 컨트롤을 집음(S2 대상).
    미발견 8은 건물 패널(물건 2=건물)·라벨 없는 칸(법정동코드/소재지) — 물건 선택 후 재대조 필요.
- **신한 2552(의뢰번호 2026233956) 매핑 다듬기 완료(2026-08-25, 사용자 확인: 금액·용도·지목 맞음)**:
  ① 물건 매칭은 순번이 아니라 **본번지·부번지**(`shinhan._row_jibun` — 물건 주소 ADDR_NEW에서 지번 추출,
  '외 N필지'·'제9층 제901호' 꼬리 제거; APW 대표지번은 폴백) + 같은 지번에 토지/건물/기계기구가 겹치면
  화면 담보세부종류로 가름(비어 있으면 첫째 + 확인 필요 표시) ② 임대 기본값 `해당없음`(추후 수정 여지)
  ③ `form._norm` 0패딩·소수 끝0 정규화(0356==356 → 일치) ④ 국민 전용 3칸(계좌·사업등록번호) 신한 매핑에서 제거.
  결과: 채울계획 15 · 일치 3 · 수동(선택형) 17 · 미발견 1(소재지 = 라벨 없는 칸, S2).
  ★2552는 7물건인데 화면 순번 1·2만 보임 — 나머지 물건 그리드 순회/추가는 미확인(2단계 전 확인 필요).
- **★발송완료 실물 대조(2026-08-25, 사용자 지정 01-2607-3-2403, 발송완료 탭 좌표 (329,29) 추가)**:
  사람이 완성·발송한 신한 폼 vs 우리 매핑 — **숫자·날짜 13칸 전부 일치**(감정평가액 17,698,428,000·총감정가액
  20,446,882,600·수수료 12,690,700·순수수료·실비·단가·면적·기준시점·답사일·본/부번지), 선택형 17 중 값 있는 것
  전부 일치(담보종류/세부/용도/지목/용도지역/물건특성/임대=해당없음). 물건 9개 그리드 순번 1~9 전부 표시.
  차이: 토지등기부번호는 사람이 비워둠(우리는 채우려 함 → 필요 여부 사용자 확인), 대표·심사자 비움,
  평가사명1은 **오른쪽 콤보**에 홍현석(라벨 매칭은 왼쪽 빈 콤보를 집음 → S3 때 컨트롤 위치 수정).
  러너 4번째 인자로 탭 지정: `run_dryrun_test.py <from> <to> <doc> 발송완료`. 발송완료 폼은 '저 장' 비활성.
- **발송 기준 반영(2026-08-25, 사용자 확정)**: 토지/건물등기부번호·대표,지사장·심사자는 **비움**(None).
  평가사명1~3은 라벨 오른쪽 **둘째** 콤보가 이름 → 라벨 키 `평가사명1@2`(form.plan_field가 '@N' 해석,
  driver.find_by_label(index=)). 테스트 152 통과.
- **★물건 행 추가 방법 정찰 완료(tools/recon_add_row.py, reports/recon_addrow_20260825_112345_menu.png)**:
  신한 폼 왼쪽 순번 그리드에서 Shift+F10 → 팝업 메뉴(#32768):
  `추가(마지막위치)(U)` · `추가(현재위치)(V)` · `추가(현재위치 복사)(W)` · `추가(마지막위치 복사)(X)` ·
  `삭제(마지막위치)(Y)` · `삭제(현재위치)(Z)`. → 행 추가 = 그리드 포커스 + Shift+F10 + 'u'(가속키, 기존 패턴).
  폼 버튼: 수정 / 계산(사정*단가) / 자동계산 / 데이터 복사·초기화 / PDF등록 / 저 장 / 작성내역출력 / 닫 기.
  ※행 추가는 폼 상태만 바뀌고 '저 장' 전엔 서버 반영 없음(추정) — 닫기 시 저장확인창 처리 확인 필요.
- **2단계 전 3개 구현 완료(2026-08-25)** — 2552 전체 물건 드라이런(reports/dryrun_20260825_113253.*)로 검증:
  ① `form.is_blank`: None/''/**숫자 0** 은 빈값(총감정가액=0 사고 방지). ② `navigate.decline_save_prompt`/
  `close_forms`: 닫기 후 확인창(#32770·TMessageForm 등)이 뜨면 '아니오/취소'만 누름(예/저장 절대 안 누름).
  실측: 행 5개 추가 후 닫아도 확인창 없이 닫혔고 추가 행은 사라짐(저장 없음 확인). ③ 물건 행 추가·선택:
  `navigate.count_object_rows/select_object_row/add_object_row(U)/ensure_object_rows`, 
  `autofill_shinhan --all-objects`: 기존 행은 본번지·부번지(+담보세부종류)로 매칭, 부족분은 U로 추가
  (실측 2→7행 OK), 행마다 물건 값 계획. 헤더(총감정가액·수수료·기준시점·답사일·평가사명)는 첫 행만.
  추가 수정: 물건순번은 화면이 매기므로 안 씀, 건물 전용 칸(사용승인일·내용/잔존연수·총층수)은 토지 행에
  안 넣음(발송 실물 기준), 기계기구는 행 값만(사용승인일 2026-08-01 — 8자리 날짜 파서 `` 버그 수정).
  결과: 채울계획 43 · 일치 4 · 수동 98 · 칸못찾음 7(소재지×7, 라벨 없는 칸) · 거부 0.
  ※ 356-26/27/28(답, 감정가 0)은 감정평가액을 안 채움(0 가드) — 의도 확인 필요.
- **★2단계 LIVE 입력 성공(2026-08-25, reports/dryrun_20260825_133734.*)**: 2552 `--live --all-objects` —
  행 2→7 추가, **43칸 입력·되읽기 검증 전부 완료, 거부 0**(헤더 6칸+물건별 면적·감정평가액·단가·본/부번지·
  건물/기계 연수·사용승인일). '저 장' 안 누르고 닫힘(확인창 없음). 수동(콤보) 98 · 소재지(라벨 없음) 7 미입력.
  ※행 순회 간헐 실패(후보 0건)가 하루 2회 — 재실행하면 됨. 원인은 클립보드 갱신 지연 추정, 보강 여지.
- **S3 콤보 자동 선택 + S2 소재지 + 순회 재시도 (2026-08-25, reports/dryrun_20260825_140040.*, diag_combo_*)**:
  - 콤보(TcxDBLookupComboBox) 선택법 실증: **안쪽 에디트 포커스 → 타이핑(접두 검색) → Enter** (`form.select_combo`).
    set_edit_text 는 ComboBoxWrapper 라 불가. 접두가 겹치면('농림지역미분류'가 '농림지역'보다 앞, '공장용지'가
    '공장'보다 앞) 닫힌 상태 ↓는 안 움직임 → **F4 열고 ↓ Enter** 반복으로 정확 항목까지 훑도록 수정(재검증 필요).
    코드 접미 '(4189)'는 비교에서 무시(_combo_norm). send_keys 특수문자 {}로 보호(_keys).
  - 2552 LIVE: **125칸 입력 완료·일치 30·거부 11**(접두 문제, 수정 후 미검증). 소재지·법정동코드(상대좌표
    POSITIONAL)는 은행 행 '일치', 추가 행 '완료'. 순회 재시도 로직 정상(1회에 44행).
  - ★**중복로그인 주의**: 실행 중 다른 PC(192.0.2.10)가 같은 ID로 로그인해 "동일 아이디로 중복로그인… 로그아웃"
    확인창(#32770, OK만 있음)이 떠 폼이 안 닫힘(저장 없이 소멸). → 실행 전 사용자와 ID 사용 조율 필요.
    decline_save_prompt 는 OK-only 창엔 손대지 않음(의도).
- **콤보 목록 기반 훑기 완성(2026-08-25 15:05, reports/dryrun_20260825_150532.*)**: `form.load_combo_lists`
  (recon/combo_shinhan.md 순서) → 현재 항목→목표 거리만큼 F4+↓/↑+Enter. 2552 LIVE **136칸 완료·일치 30·거부 0**
  ('공장' 40/79번 항목까지 정확 선택). 새 ID(.env 교체)로 중복로그인 없이 정상 닫힘.
  ★남은 버그: 평가사명1@2 가 **왼쪽 콤보**에 들어감(find_by_label 후보에 안쪽 에디트가 섞여 index 어긋남) →
  Inner 제외로 수정 → **재검증 OK**(reports/dryrun_20260825_151120.*: 오른쪽 콤보에 김치암, 136칸·거부 0).
- **★2단계 현장조사서 작성(E) 구현·LIVE 검증(2026-08-25, reports/survey_20260825_154629.*, recon_survey_*)**:
  폼 TBNKSHG24Hyun '담보 현장조사서'(12칸): 현장답사일·교통비·토지조사비·물건조사비·임대차조사비·공부발급비·
  기타실비·특별용역비·사진비·예외적용(콤보)·부가세·실비합계 + 자동계산/저 장/닫 기.
  매핑(`mapping/survey.py`, 발송 실물 2403으로 확정): 교통비=APW_Bill.YEBI, 토지조사비=TOJOSABI,
  물건조사비=MULJOSABI, 공부발급비=GONGBU, **사진비=SILBI**(APW '기타실비' 컬럼이 화면 사진비), 특별용역비=YONGYEUK,
  부가세=SILBITAX, 실비합계=SILBISUM+SILBITAX(담보 폼 '실 비'와 동일), 현장답사일=context.survey_date,
  예외적용='미적용'. 임대차조사비·기타실비는 비움. 0은 가드로 비움(사람도 비움).
  `navigate.open_survey_form`(Shift+F10+'e')·`survey_forms`·`close_survey_forms`, `tools/autofill_survey.py`,
  `tools/run_survey_test.py <from> <to> <doc> [탭] [--live]`.
  2676 LIVE: **7칸 입력·일치 1·거부 0**, 닫기 시 확인창 없음, 남은 폼 0. (2552는 15:40경 발송돼 작성 탭에서 빠짐.)
  ※실비합계 되읽기 '57200' 뒤에 커서 잔상 — 값은 정상. 저장은 미시도(연습건 대기).
- **★★3단계 저장 성공(2026-08-25 16:50, 01-2608-3-2676, reports/save_20260825_165029.*, prompt_*.png)** —
  `tools/run_save_test.py <from> <to> <doc> [--dry|--survey-only|--overwrite]`(발송 없음):
  행 찾기 → 작성(A) LIVE 19칸 → '저 장' → "저장 되었습니다"(OK) → 닫기 → **포커스 확인(focused, 재순회 없음)**
  → 현장조사서(E) LIVE 5칸 → 저장(OK) → 닫기 → 재개봉 대조: 작성 폼 일치 32/채울 3(은행값 유지한 매매/분양·소재지·임대),
  현장조사서 일치 7. 저장 확인창은 #32770 OK-only("저장 되었습니다"), `navigate.save_form/accept_prompt`(저장 흐름 전용).
  발견: Bank24 가 저장 시 실비합계를 항목합×1.1 로 재계산(58,190) — APW SILBISUM/SILBITAX(52,000/5,200)는 사진비 등이
  빠져 있음 → `survey.build` 를 항목합 기반(부가세 5,290)으로 수정, 2676 부가세 **재저장 완료**(16:53, --survey-only --overwrite, 재대조 8/8 일치).
  ★러너는 반드시 `-WindowStyle Hidden` 으로: 콘솔 QuickEdit 선택 모드("선택 …" 제목)에 걸리면 print 에서 영구 블록됨(16:38 실측).
  미해결: 매매/분양 우리값 '예' vs 은행값 '아니오'(2676) — 판정 규칙 확인 필요. 소재지 _dong_only 가 건물명 꼬리 못 뗌.
  평가단가는 건물 물건에서 빈칸(매핑 없음).
- **PDF등록 파악(2026-08-25 17:07, 사용자 요청: 신한 작성 폼 PDF등록 → 이후 국민(작성+현장조사서))**:
  - 전례 공유폴더 규칙 → `sources/archive.py find_pdf(doc)` (테스트 7개, 실서버 9건 중 8건 적중):
    `\jun\JUN\{연도폴더}\{구분}\{범위}\{건폴더}\{감정서번호}.pdf`. 연도폴더 '0-2026년 on-line전례'(옛 연도는 이름 제각각 →
    'YYYY년'+'전례' 포함 폴더), 구분 = 번호 3번째 마디(3→'3-담보', 0~9 표 있음), 범위 500단위 '2501~3000',
    건폴더는 번호 접두(꼬리 '(HUG)'·'주금공' 등 허용, 대시누락형 허용), 파일은 정확 이름 우선·유일할 때만 느슨 매칭.
    잘못 넣은 범위 폴더 폴백. 재발행은 '012601-3-0136-1' 처럼 별도 번호(-1)라 원번호로는 못 찾음(정상).
    NAS '#recycle' 은 stat 만 해도 WinError 59 → _is_dir 로 흡수. ★Git Bash 의 python 은 UNC 접근 불가, PowerShell 의 python 은 됨.
    2026 담보 2,332폴더: 폴더명=번호인 1,923건은 exact pdf 있음, 나머지는 꼬리 폴더명(HUG 등) — 규칙으로 커버.
  - 작성 폼 'PDF등록' 버튼(TcxButton) → Bank24 자체 창 **TBnkTop24pdf '감정평가서(PDF) 첨부'**(탭 '(원본)감정평가서'·
    '평가전례 첨부용', 버튼 '파일선택'/'별도창으로 열기'/'닫 기', 안내 "File Size 500MB 제한", 미리보기 영역).
    '파일선택' → 표준 열기 대화상자 **#32770 '첨부할 감정평가서 파일 선택'**(파일 이름 Edit + '열기(&O)'/'취소',
    필터 PDF Files, 초기 폴더 \data\DATA.업무2팀\온라인탁상) → 파일명 칸에 전체 경로 넣고 '열기'로 선택 가능.
    ★미확인: 열기 후 자동 업로드인지 / 별도 등록 버튼·확인창이 있는지 / '닫 기'로 확정되는지 → 2676 실등록 시험 필요(허락 대기).
  - 정찰 도구: tools/recon_pdf_button.py, tools/recon_pdf_dialog.py (읽기 전용, 취소로 끝냄).
- **★PDF등록 성공(2026-08-25 17:16, 2676, reports/pdf_20260825_171603.*)**: `navigate.open_pdf_window/attach_pdf/
  close_pdf_windows` + `tools/run_pdf_test.py <from> <to> <doc> [--pdf 경로]`. 흐름: PDF등록 → 파일선택 → 열기 대화상자
  '파일 이름' Edit(가장 아래 Edit)에 전체 UNC 경로 set_edit_text → '열기(&O)' BM_CLICK → **확인창 없이 약 5초 만에 업로드·
  미리보기 표시**(Acrobat AVL_AVView, 70쪽, 표지에 01-2608-3-2676) → '닫 기'(확인창 없음) → 폼 닫고 다시 PDF등록 열면
  같은 PDF 가 그대로 떠 있음 = 서버 등록 확정. 별도 등록 버튼 없음. '평가전례 첨부용' 탭은 미사용.
  ※열기 대화상자 초기 폴더가 \data\DATA.업무2팀\온라인탁상 이지만 전체 경로를 넣으면 무관.
- **통합 러너 `tools/run_shinhan_full.py <from> <to> <doc> [--dry|--no-pdf|--pdf 경로|--overwrite|--skip-write|--skip-survey]`
  (2026-08-25 17:32, 드라이런 배선 검증 OK: reports/full_20260825_173240.*)**: 작성 열기→입력→저장→PDF등록→닫기→포커스 확인→
  현장조사서→입력→저장→닫기→검증(재개봉 대조·PDF 뷰어 유무). PDF 경로는 화면 건드리기 전에 확정(못 찾으면 시작 안 함).
  PDF등록 뒤 재저장은 불필요(등록 즉시 확정 실측).
  ★담당자 검증 중 관찰(2676, 17:32 재대조): 담당자가 **매매/분양 '예'·임대 '임대없음(공실)'로 수정 = 우리 매핑값과 동일**
  (은행 기본값을 안 덮어쓴 정책 때문에 못 넣었던 것) / **별도 등기 존재 '예'**(우리는 아니오 — 근거 소스 미확인) /
  **내용연수·잔존연수 비움**(오피스텔 구분건물엔 안 넣는 듯 — 우리는 50/50 채움). → 물건특성은 은행 기본값 덮어쓰기 허용
  여부, 구분건물 연수 규칙, 별도등기 판정 근거를 사용자에게 확인할 것.

### 다음 (번호 없이 가능)
1. S2 라벨 없는 칸 좌표/순서 식별 — `dump --row N` 으로 신한/국민 행 골라 폼 열고 정찰
   (rows 출력의 은행 셀로 골라서 N 지정).
2. S3 선택형 v2 — 콤보 click_input 함정 여부(diag_radio2 방식 스크린샷 대조), 저장 없이 닫기.
### 연습건 번호 필요
- S4 실입력·저장(--live), S5 발송(B), 현장조사서 E/G.

## 2026-08-24 진행분

- **S1 완료**: `tools/autofill_shinhan.py` — autofill_kb의 신한판(기본 드라이런, `--live`만 실입력).
- **query/scan 추가 + 실화면 검증 완료**:
  - `python -m bankon.cli query --from 2026-08-18 --to 2026-08-18 --work-type 담보`
    — '찾 기'(감정서번호 검색, **사용자가 불신·사용 금지**) 없이 업무구분·기간으로 목록 조회.
  - `open/dump`에 `--write-tab`(작성 탭 선택)·`--from`(조회시작일)·`--scan`(조회 목록을
    행 단위로 훑어 문서 찾기) 옵션 추가. **--scan 실화면 시험은 아직 안 함** ← 다음 할 일.
- **★함정 발견: DevExpress 라디오는 click_input이 안 먹힌다** (포커스만 바뀌고 선택 안 됨).
  `driver.click_message()`(BM_CLICK 메시지)로 해야 함 — `tools/diag_radio2.py`로
  3방식(마우스✗/BM_CLICK✔/포커스+스페이스✔) 스크린샷 대조 실증. 선택 상태는 UIA/MSAA로
  못 읽으므로 **검증은 그룹 스크린샷 판독**(run_query_test.py가 조회 후 자동 촬영).
- 러너: `tools/run_query_test.py` — 관리자 권한 필수(UIPI), 실행법:
  `Start-Process python -ArgumentList '"...\tools\run_query_test.py"' -Verb RunAs -Wait`
  로그·스냅샷은 `reports/query_test_*.{log,png}`.
- 진단 도구: `tools/diag_radio.py`(상태 비트 읽기 — 결론: 못 읽음),
  `tools/diag_radio2.py`(클릭 3방식 스크린샷 대조 — 재사용 가능).

### 내일 바로 이어서 (순서)

1. **`open --write-tab --from 2026-08-18 --scan <감정서번호>` 실화면 시험** — 대상 번호는
   사용자에게 받기(01-2608-3-2615는 사용자가 검색 금지시킴 — '찾 기' 경유라서였음. scan은
   '찾 기'를 안 쓰지만 대상 번호는 다시 확인받을 것).
2. `find_row_by_doc` 행 순회가 실그리드에서 동작하는지(정찰 때 Ctrl+A가 1행만 반환하던 문제).
3. 그다음 PLAN.md S2(라벨 없는 칸)·S3(선택형 v2 — 콤보도 click_input 함정 여부 확인할 것).

## 바로 시작하는 법

```bash
cd D:\AI\BankOn
python -m pytest -q                                   # 121개 통과해야 정상

# 화면 열기 (BANK24 실행→로그인→검색→작성 폼)
PYTHONPATH=src python -m bankon.cli open 01-2606-3-1887

# 열린 폼과 우리 매핑 값 대조 (읽기 전용)
python tools/verify_form.py 01-2606-3-1887 --live     # FTP 실시간
python tools/verify_form.py 01-2607-3-2182            # GamJun DW (백필된 건만)
```

`.env` 는 프로젝트 안에 있고 `.gitignore` 로 제외돼 있다.

---

## A. 남은 불일치 6건 (다듬기)

| # | 항목 | 은행 | 원인 / 할 일 |
|---|---|---|---|
| 1 | **용도지역 축약** | 국민 2건 | 화면 `2종일반주거`·`보전관리` vs `.gam` `제2종일반주거지역`·`보전관리지역`.<br>→ 국민 폼 열고 `python tools/list_combo_items.py --handle 0x… --probe --only 용도지역` 로 목록 수집 후 별칭표 작성 |
| 2 | **물건종류(114개 콤보)** | 국민 2건 | 지목 유추가 신뢰 불가(명세가 토지뿐인데 화면은 `일반상가`인 사례).<br>→ **사용자에게 판정 규칙을 받아야 함.** 지금은 의도적으로 비워둠 |
| 3 | **등기번호** | 국민 1건 | `T_SCAN_GONGBU` 행 선택이 물건과 안 맞음(`…0777` vs `…0980`).<br>→ 주소/면적으로 매칭하는 방법 검토 |
| 4 | **건물구조·사정면적** | 국민 1건(2449) | `section_build` 컬럼 매핑 오류 — 구조 칸에 `제3종`(용도지역)이 들어옴.<br>→ `parse/detail.py` `TEXT_COLUMNS["struct"]` 후보 재확인 필요 |
| 5 | 물건특성 | 신한 1건 | 의견서에 표가 없는 건 — **소스에 정보가 없어 해결 불가**(기본값 유지) |

## B. 입력 드라이버 (본 작업)

1. `ui/form.py` — 라벨로 컨트롤 찾아 값 주입 + **되읽기 검증**
   - `driver.set_text` 이미 있음(안쪽 `TcxCustomInnerTextEdit` 에 써야 함)
   - 콤보는 이름으로 선택(표시값의 `(코드)` 는 무시해도 됨)
   - **라벨 없는 칸**(국민 법정동코드·소재지, 신한 REG/EUB·우편번호)은 좌표/순서로 지정
2. **국민은 물건 추가가 선행** — 작성 전 건을 열면 물건 패널이 없다(컨트롤 48개).
   화면 안내: "해당건을 선택 후 우클릭 하시면 일괄 추가"
3. 점검항목은 `TcxComboBox`(비DB) 를 **화면 순서대로** 채운다 —
   국민은 `KB_Summary_Chk` a1~a12/b1~b5/c1~c3 순서와 일치, 신한은 전부 `1`
4. `AUTO_SUBMIT=1` 이면 `저 장` 까지 누른다(발송 아님 — 사용자 확인함)
5. **연습용 건으로 먼저 시험할 것.** 값이 실제로 바뀐다

## C. 은행 확대

담보만 대상(탁상·동산담보·공동주택자문 제외). 새 은행은 이 순서로:
```bash
python tools/inspect_bankon.py list
python tools/map_fields.py --handle 0x…              # 항목표 → recon/
python tools/list_combo_items.py --handle 0x… --probe # 콤보 목록 → recon/
```
그 다음 `mapping/<bank>.py` 추가. 파서·조회는 그대로 재사용된다.

---

## 사용자에게 물어볼 것

1. **국민 `물건종류`(114개) 판정 규칙** — 실무에서 무엇을 보고 고르는지
2. 신한 `DABO_JONG_SUB` 미매칭 2종: `근생시설 및 다가구`, `양도담보` → 어느 코드로?
3. 입력 시험용으로 **버려도 되는 건** (값이 바뀜)

## 참고 — 오늘 고친 것 중 잊기 쉬운 것

- **FTP 계정은 `BonJisa`** (`BonJisa16` 아님). `D:\AI\GamJun\.env` 도 같이 고쳤고
  원본은 `.env.bak-20260819`. **GamJun 증분 배치도 이걸로 막혀 있었을 것** — 서버 쪽 확인 필요
- 사업등록번호 `2148746436`
- 은행마다 수수료 계산이 다르다(신한=TOTAL, 국민=순수수료 1,000원 절사 후 10%)
