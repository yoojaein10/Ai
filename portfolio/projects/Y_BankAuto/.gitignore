# 실제 PDF/스프레드시트/CSV/로그 및 파싱 산출물 — 절대 커밋 금지
*.pdf
*.xlsx
*.xls
*.csv
*.log

# 실제 은행명/업무 데이터 폴더 (실데이터·PII 포함 가능)
국민은행/
기업은행/
농협은행/
농협중앙회/
새마을금고/
수협은행/
신한은행/
우리은행/
주택도시보증공사/
하나은행/
다필지/
탁상/
탁상_작업/

# 산출물·임시 디렉터리
output/
outputs/
tmp/
reports/
dist/
build/

# 합성 테스트 픽스처(_test_*.py)는 위 확장자 규칙에 걸리지 않으므로 그대로 추적된다.
# 만약 의도적으로 추적하는 합성 데이터 파일이 생기면, 무조건적 확장자 규칙 대신
# 아래처럼 구체적 예외(negation)를 추가한다:
#   !fixtures/REDACTED_CONFIGURE_LOCALLY_sample.csv
